import React from 'react';
import Game from './Game';

const App = props => {
  return (
    <div className="ui container masthead center aligned vertical">
      <Game />
    </div>
  );
};

export default App;
